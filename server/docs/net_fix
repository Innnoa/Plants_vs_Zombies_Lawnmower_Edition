P0
  这一阶段的目标是先把“跳变、丢状态、多人不同步感明显”压下去。这里优先
  级最高，而且大部分都可以先只改服务器。

  - P0-1 UDP 同步分块
    做法：给 S2C_GameStateDeltaSync 和必要的 S2C_GameStateSync 增加单包
    大小预算，超限就按对象批次拆成多包发送。
    解决：敌人多时 UDP 包过大、IP 分片、丢一片整包失效。
    范围：服务器可独立完成。
    收益：这是当前最直接影响“跳变”的点，必须第一个做。
  - P0-2 统一同步时序规则
    做法：所有状态包和关键事件包统一使用 tick 作为主排序依据，服务端明
    确“同一对象只接受更高 tick 覆盖”的协议语义。
    解决：TCP 事件 + UDP 状态 混发时的乱序风险。
    范围：服务器先做，客户端后续必须对齐消费规则。
    收益：这是“看起来莫名其妙”问题的主要根源之一。
  - P0-3 缩短收敛窗口
    做法：把强制全量快照窗口改短，并给关键对象增加“短期续发”机制，比如
    新生成敌人、新掉落道具、玩家死亡、复活、拾取。
    解决：delta 丢包后长时间不收敛。
    范围：服务器可独立完成。
    收益：从“丢一次错几秒”变成“丢一次很快拉回”。
  - P0-4 关键事件按帧合包
    做法：把受伤、死亡、升级、掉落这类同 tick 内多条事件按类型批量打
    包，减少单帧 TCP 小包数量。
    解决：怪多时事件碎片太多、TCP 写队列抖动。
    范围：服务器可独立完成，客户端若当前已支持批量结构则无需改；否则要
    补消费。
    收益：多人战斗时更稳。
  - P0-5 修正全量语义一致性
    做法：凡是逻辑上属于全量快照的 S2C_GameStateSync，都明确
    is_full_snapshot=true。
    解决：开局/重连/强制全量的协议语义不统一。
    范围：服务器可独立完成，客户端建议按该标志严格处理。
    收益：避免以后继续出现“客户端把增量当全量”或者相反的问题。

  P1
  这一阶段的目标是把“敌人多时仍稳定”和“多人时更顺”做上去。

  - P1-1 AOI 或最小版同步裁剪
    做法：先不做复杂视野系统，先做“距离玩家较远的敌人降频同步”或“非关键
    对象按分层频率同步”。
    解决：全房间全量广播导致的带宽和序列化膨胀。
    范围：服务器可独立完成。
    收益：敌人数量一上来，收益会非常明显。
  - P1-2 网络与重逻辑解耦
    做法：至少把重型 tick 构建和网络发送做更明确的阶段解耦；更进一步可
    以把重逻辑调度从主网络回调压力里剥出去。
    解决：某一帧逻辑重时，网络也跟着卡一下。
    范围：服务器可独立完成。
    收益：整体“顺滑感”提升，不只是平均帧时好看。
  - P1-3 每房间同步预算与背压
    做法：给每个房间设置单 tick 最大发包预算、最大事件条数、最大同步对
    象数，超出则延后或降级。
    解决：极端局面下某个房间把整个网络发送链拉爆。
    范围：服务器可独立完成。
    收益：稳定性上限提升，尤其多人房间。
  - P1-4 客户端消费对齐
    做法：客户端必须按 tick 去旧、禁止旧包覆盖新状态、对 delta 做幂等更
    新、对全量快照做覆盖式收敛。
    解决：服务器已正确发包但客户端仍表现乱序、回跳。
    范围：需要客户端对齐。
    收益：这是最终体感稳定的必要条件。

  P2
  这一阶段是把体系从“已经稳很多”继续推进到“更强上限”。

  - P2-1 完整预测回滚配套
    做法：把现在已有的 input_seq + history 真正接成回滚校验和纠偏，而不
    是只做历史缓存。
    解决：高延迟、突发乱序时的位置偏差和输入确认滞后。
    范围：服务器为主，客户端也要配合预测策略。
    收益：主要提升高延迟环境，不是当前第一优先。
  - P2-2 频道化消息体系
    做法：把消息明确分成“可靠控制”“可靠关键事件”“不可靠高频状态”三类，
    必要时再引入更明确的可靠 UDP 或应用层重传策略。
    解决：现在 TCP/UDP 双通道语义比较松散的问题。
    范围：协议层改动，双端都要动。
    收益：体系更清晰，但改动面大。
  - P2-3 序列化与内存池优化
    做法：继续减少临时对象、批量复用缓冲、按房间缓存序列化结果。
    解决：高并发下的分配和拷贝抖动。
    范围：服务器可独立完成。
    收益：偏性能优化，不是当前主矛盾。
