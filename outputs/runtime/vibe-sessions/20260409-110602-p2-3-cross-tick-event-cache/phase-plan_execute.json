{
  "run_id": "20260409-110602-p2-3-cross-tick-event-cache",
  "status": "completed",
  "completed_scope": [
    "延迟非关键事件 backlog packet 增加持久 prepared bytes 槽",
    "backlog 入队时预先 materialize TCP framed bytes",
    "backlog 跨 tick drain 时直接复用 packet 自带 prepared bytes",
    "新增 backlog prepared bytes identity 测试辅助接口",
    "event_dispatch_backlog 单元测试覆盖跨 tick identity 与 drain 清理"
  ],
  "not_in_scope": [
    "状态同步跨 tick bytes 复用",
    "客户端改动",
    "协议字段变更",
    "protobuf 对象池化"
  ],
  "verification": [
    {
      "command": "env CCACHE_DISABLE=1 script/build_server.sh --debug",
      "result": "passed"
    },
    {
      "command": "ctest --test-dir server/build-debug --output-on-failure -R event_dispatch_backlog",
      "result": "passed"
    },
    {
      "command": "ctest --test-dir server/build-debug --output-on-failure -R \"dispatch_cache|event_dispatch_backlog|channel_policy|server_smoke|udp_sync_smoke|tcp_event_batch_smoke|state_packet_budget_smoke|delta_packet_budget_smoke|event_backpressure_smoke|room_sync_budget_smoke|full_snapshot_chunking_smoke\"",
      "result": "passed"
    }
  ],
  "residual_risks": [
    "状态同步链路仍按 dispatch tick 重写 timing，因此跨 tick prepared bytes 复用仍只覆盖事件 backlog",
    "protobuf 对象池化仍未纳入当前范围"
  ]
}
